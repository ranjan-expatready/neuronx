{
  "name": "@neuronx/http",
  "version": "1.0.0",
  "description": "HTTP client utilities with circuit breaker, retry, and rate limiting",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^0.34.0"
  }
}
